global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "finsight-observer"
    static_configs:
      - targets: ["finsight-observer:9090"]
    metrics_path: /metrics
    scrape_interval: 10s
